#include <stdio.h>
#include <locale.h>

/* Escreva um algorítimo que determine se um candidato a doação de sangue pode realizar a doeação com base nos critérios:
 - Ter entre 16 e 69 anos de idade.
  - Pesar ao menos 50kg */ 

int main () {
	int contador = 1; // ---> VÁRIAVEL DE CONTROLE DA QUANTIDADE
	//usando o laço while
	while (contador <= 10) /* ---> CONDIÇÃO DE PARADA DA REPETIÇÃO (BOOLEAN) */ {
		printf("Hello Word!");
		contador++; // ---> FATOR DE INCREMENTO/DECREMENTO
	} printf("\n\n");
	// usando o laço for
	for (contador = 1;/* ---> VÁRIAVEL DE CONTROLE DA QUANTIDADE */ contador /* ---> CONDIÇÃO DE PARADA DA REPETIÇÃO (BOOLEAN) */ <= 10; contador++ /* ---> FATOR DE INCREMENTO/DECREMENTO */) {
		
	}
}
